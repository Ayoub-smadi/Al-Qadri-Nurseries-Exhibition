{
  "name": "workspace",
  "version": "0.0.0",
  "license": "MIT",
  "scripts": {
    "dev": "concurrently --names \"API,WEB\" --prefix-colors \"cyan,green\" \"cross-env PORT=8080 pnpm --filter @workspace/api-server run dev\" \"cross-env PORT=5000 BASE_PATH=/ pnpm --filter @workspace/nursery-showcase run dev\"",
    "dev:api": "cross-env PORT=8080 pnpm --filter @workspace/api-server run dev",
    "dev:web": "cross-env PORT=5000 BASE_PATH=/ pnpm --filter @workspace/nursery-showcase run dev",
    "db:push": "pnpm --filter @workspace/db run push",
    "build": "pnpm run typecheck && pnpm -r --if-present run build",
    "typecheck:libs": "tsc --build",
    "typecheck": "pnpm run typecheck:libs && pnpm -r --filter \"./artifacts/**\" --filter \"./scripts\" --if-present run typecheck"
  },
  "private": true,
  "pnpm": {
    "onlyBuiltDependencies": ["core-js", "esbuild", "esbuild-plugin-pino"]
  },
  "dependencies": {
    "cors": "^2",
    "express": "^5",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "@types/cors": "^2.8.19",
    "@types/express": "^5.0.6",
    "@types/pg": "^8.18.0",
    "concurrently": "^9.1.2",
    "cross-env": "^7.0.3",
    "typescript": "~5.9.2",
    "prettier": "^3.8.1"
  }
}
