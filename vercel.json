{
  "version": 2,
  "buildCommand": "pnpm install --filter @workspace/nursery-showcase... --filter @workspace/api-server... --frozen-lockfile && NODE_ENV=production pnpm --filter @workspace/nursery-showcase run build",
  "outputDirectory": "artifacts/nursery-showcase/dist/public",
  "rewrites": [
    { "source": "/api/:path*", "destination": "/api/index" },
    { "source": "/((?!api).*)", "destination": "/index.html" }
  ]
}
